/**
 * Compatibility barrel only.
 *
 * Keep this file so existing relative imports remain stable while TypeScript
 * resolves one canonical model from `src/types/index.ts`.
 */
export * from './types/index';
